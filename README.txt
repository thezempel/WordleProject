pip install flask
pip install flask_cors
pip install flask_restful
pip install pymysql


in config.py:
change password to personal mysql password

to load words:
run 'loadwords.py'
*make sure db and password are correct in config
